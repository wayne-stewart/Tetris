{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "debug build server",
            "type": "shell",
            "command": "cargo",
            "args": ["build"],
            "group": { "kind": "build", "isDefault": true },
            "options": {
                "cwd": "${workspaceFolder}/webserver"
            }
        }
    ]
}